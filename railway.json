{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "releaseCommand": "npx prisma migrate deploy && npm run postdeploy:detail-payload",
    "startCommand": "npm start",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
